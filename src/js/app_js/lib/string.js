/**
 * This doesn't do anything and is only mean to test breakpoints and mapping
 * in a normal Javascript file.
 */
exports.hello = function(name) {
  var s = "H";

  s += "ello, ";

  return s + name;
};
